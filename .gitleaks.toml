title = "Mac Health Checkup gitleaks configuration"

[extend]
useDefault = true

[[allowlists]]
description = "Ignore detect-secrets baseline metadata file (stores hashed fingerprints, not raw secrets)."
paths = ['''^\.secrets\.baseline$''']
